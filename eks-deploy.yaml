apiVersion: apps/v1
kind: Deployment
metadata:
  name: eks-foodies-linux-deployment
  namespace: eks-foodies-app
  labels:
    app: eks-foodies-linux-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: eks-foodies-linux-app
  template:
    metadata:
      labels:
        app: eks-foodies-linux-app
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/arch
                operator: In
                values:
                - amd64
                - arm64
      containers:
      - name: nginx
        image: <My Docker Image here>
        ports:
        - name: http
          containerPort: 80
        imagePullPolicy: IfNotPresent
      nodeSelector:
        kubernetes.io/os: linux